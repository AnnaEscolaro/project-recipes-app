export default function RecipeInProgress({ path }:{ path: string }) {
  // buscar a chave InProgress do localStorage e renderizar cada receita que está lá

  // adicionar o botão de finalizar receita pra cada uma deles

  // salvar no localStorage o progresso referente a cada ingrediente da receita

  return (
    <div>In Progress</div>
  );
}
